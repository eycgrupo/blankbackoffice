<?xml version="1.0" encoding="UTF-8"?>
<domain-models xmlns="http://axelor.com/xml/ns/domain-models"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://axelor.com/xml/ns/domain-models https://axelor.com/xml/ns/domain-models/domain-models_6.1.xsd">

  <module name="base" package="com.eyc.base.db" />

   <entity name="Customer" cacheable="false">

   <string name="firstName" title="First Name"   />
   <string name="lastName"  title="Last Name"   />
   <string name="fullName" namecolumn="true" title="Full name" >
     <![CDATA[
            if (firstName == null && lastName == null)
              return null;
            if (lastName == null)
                    return  firstName;
            if (firstName == null)
                    return  lastName;
            return  firstName + " " + lastName;
     ]]> 
   </string>

   <boolean name="active" title="is Active?"  default="true" />
   <decimal name="currentBalance" title="Current Balance"
         precision="12" scale="2"  default="0" />
   <one-to-many name="payments" ref="Payment"  mappedBy="customer" title="Payments" />
 </entity>
 
</domain-models>